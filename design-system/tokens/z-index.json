{
  "$schema": "https://design-tokens.github.io/community-group/format.json",
  "zIndex": {
    "base": {
      "$type": "number",
      "$value": 0,
      "$description": "Standard content layering"
    },
    "header": {
      "$type": "number",
      "$value": 100,
      "$description": "Sticky site header"
    },
    "tooltip": {
      "$type": "number",
      "$value": 150,
      "$description": "Non-blocking context information popups"
    },
    "drawer": {
      "$type": "number",
      "$value": 200,
      "$description": "Side navigation drawers and panels"
    },
    "modal": {
      "$type": "number",
      "$value": 300,
      "$description": "Blocking dialogs and wallet selections"
    },
    "toast": {
      "$type": "number",
      "$value": 400,
      "$description": "Transient notification alerts and snackbars"
    }
  }
}
